import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Contact | VibeX",
  description: "How to reach VibeX — founder, GitHub, and support channels.",
};

export default function ContactLayout({ children }: { children: React.ReactNode }) {
  return children;
}
